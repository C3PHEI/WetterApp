<Window x:Class="WetterApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WetterApp"
        mc:Ignorable="d"
        Title="Wetter App" Height="550" Width="800" MinWidth="500" MinHeight="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <!-- Header -->
        <Border Grid.ColumnSpan="2" Grid.Row="0" Background="LightBlue" Padding="20">
            <TextBlock Text="Open Weather API" 
                       FontSize="36" 
                       FontWeight="Bold"
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"/>
        </Border>
        
        <!-- Suchbereich -->
        <StackPanel Grid.Row="1" 
                    Orientation="Vertical" 
                    HorizontalAlignment="Left"
                    Margin="30,30,20,20">
            <StackPanel Background="LightBlue"
                        Orientation="Horizontal"
                        Margin= "0,10,10,10">
                <Label Content="Latitude:" 
                       VerticalContentAlignment="Center"
                       FontSize="14"
                       FontWeight="SemiBold"/>
                <TextBox Width="150" 
                         MinWidth="100"
                         Height="35"
                         VerticalContentAlignment="Center"
                         Padding="10,0"
                         Margin="13,0,0,0"
                         FontSize="14"/>
            </StackPanel>
            <StackPanel Background="LightBlue"
                        Orientation="Horizontal"
                        Margin= "0,10,10,10">
                <Label Content="Longitude:" 
                       VerticalContentAlignment="Center"
                       FontSize="14"
                       FontWeight="SemiBold"/>
                <TextBox Width="150" 
                         MinWidth="100"
                         Height="35"
                         VerticalContentAlignment="Center"
                         Padding="10,0"
                         FontSize="14"/>
                </StackPanel>
        </StackPanel>
        
        <Button Grid.Column="1" Grid.Row="1" 
                Content="Suchen" 
                Width="100"
                Height="35"
                Margin="0,0,30,0"
                FontSize="14"
                FontWeight="SemiBold"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"/>
        
        <!-- output -->
        <Border Grid.ColumnSpan="2" Grid.Row="2" 
                Background="#F5F5F5" 
                Margin="10,10,10,10"
                Padding="20,0,20,20">
            <Label Content="12345"
                   Background="Silver"/>
        </Border>
    </Grid>
</Window>